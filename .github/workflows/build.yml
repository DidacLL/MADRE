name: MADRE Lane C validation

on:
  pull_request:
  push:
    branches: [main, 'agentic/lane-c-engine-contract-repair']
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  linux-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
      - name: Install native build dependencies
        run: sudo apt-get update && sudo apt-get install -y libsqlite3-dev libcurl4-openssl-dev
      - name: Build native Kernel and deterministic process fixture
        run: |
          cmake -S kernel -B kernel/build -DCMAKE_BUILD_TYPE=Release
          cmake --build kernel/build --parallel 2
      - name: Run native unit tests
        run: ctest --test-dir kernel/build --output-on-failure
      - name: Build Java physical client and acceptance harness
        run: ./gradlew --no-daemon clean build
      - name: Prove LCR2 external-inference baseline
        run: |
          python3 ./kernel/tests/lcr2_acceptance.py \
            ./kernel/build/madre-kernel \
            ./kernel/build/madre-process-fixture \
            'madre-kernel-client/build/classes/java/main:madre-kernel-client/build/classes/java/test'
      - name: Prove LCR3 physical substrate closure
        run: |
          python3 ./kernel/tests/lcr3_acceptance.py \
            ./kernel/build/madre-kernel \
            ./kernel/build/madre-process-fixture \
            'madre-kernel-client/build/classes/java/main:madre-kernel-client/build/classes/java/test'
      - name: Audit production Kernel for provider/runtime contamination
        run: |
          if grep -RniE 'llama|GGUF|Ollama|OpenAI|Anthropic|Gemini|LM Studio|vLLM|Hugging Face' kernel/src madre-kernel-client/src/main/java; then
            echo 'Provider/runtime-specific production Kernel contamination found' >&2
            exit 1
          fi
          if grep -RniE 'EngineDescriptor|engine_inventory|WorkerPool|warm worker|required_capabilities|eligible_engine_ids|exact_model_id|exact_engine_id|Effort|provider registry|engine registry' kernel/src madre-kernel-client/src/main/java; then
            echo 'Rejected inference-ownership abstraction found in active production sources' >&2
            exit 1
          fi
          if grep -RniE 'input_path|request\.input|acknowledge' kernel/src madre-kernel-client/src/main/java; then
            echo 'LCR1 global-payload or result-only acknowledgement fossil found' >&2
            exit 1
          fi

  windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
      - name: Install native dependencies
        shell: pwsh
        run: vcpkg install sqlite3:x64-windows-static curl:x64-windows-static
      - name: Build native Kernel and deterministic process fixture
        shell: pwsh
        run: |
          cmake -S kernel -B kernel/build "-DCMAKE_TOOLCHAIN_FILE=$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake" -DVCPKG_TARGET_TRIPLET=x64-windows-static
          cmake --build kernel/build --config Release --parallel 2
      - name: Run native unit tests
        shell: pwsh
        run: ctest --test-dir kernel/build -C Release --output-on-failure
      - name: Build Java physical client and acceptance harness
        shell: pwsh
        run: .\gradlew.bat --no-daemon clean build
      - name: Prove LCR2 Windows physical baseline
        shell: pwsh
        run: |
          python .\kernel\tests\lcr2_acceptance.py `
            .\kernel\build\Release\madre-kernel.exe `
            .\kernel\build\Release\madre-process-fixture.exe `
            'madre-kernel-client\build\classes\java\main;madre-kernel-client\build\classes\java\test'
      - name: Prove LCR3 Windows physical substrate closure
        shell: pwsh
        run: |
          python .\kernel\tests\lcr3_acceptance.py `
            .\kernel\build\Release\madre-kernel.exe `
            .\kernel\build\Release\madre-process-fixture.exe `
            'madre-kernel-client\build\classes\java\main;madre-kernel-client\build\classes\java\test'
