data_dir = ".madre-data"
host = "127.0.0.1"
port = 8731

[capabilities.local-chat]
kind = "openai_chat"
endpoint = "http://127.0.0.1:11434/v1"
model = "local-model"
provider_id = "local-openai-compatible"
boundary = "local"
latency_class = "standard"
quality_tier = "standard"
reasoning_efforts = ["low", "medium", "high"]
paid = false
heavyweight = true
# This example assumes inference stays in the user's local MADRE environment.
# Local transport alone does not establish MODULE_PRIVATE or SECRET containment.
privacy = 3
# api_key_env = "OPENAI_API_KEY"  # optional for providers that require it
