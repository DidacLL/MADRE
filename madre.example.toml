data_dir = ".madre-data"
host = "127.0.0.1"
port = 8731

[capabilities.local-chat]
kind = "openai_chat"
endpoint = "http://127.0.0.1:11434/v1"
model = "local-model"
boundary = "local"
heavyweight = true
trust = 5
risk = 1
scopes = ["local-model"]
# api_key_env = "OPENAI_API_KEY"  # optional for providers that require it
