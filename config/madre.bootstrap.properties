# Shipped first-run installation policy for the current MADRE owner product foundation.
# Host-owned paths are written by MADRE at first launch and are intentionally absent here.
# The selected ordinary CORE Module is also the current owner-interaction Module; there is no
# duplicated interaction Module identity and no CORE execution/security privilege.
roles.core=io.github.didacll.madre.owner-interaction
kernel.result-retention-seconds=86400
resources.model-slot=1

interaction.default-operation=fast-lane
interaction.standard-operation=standard-prompt
interaction.prompt-material-type=owner-prompt
interaction.default-sensitivity=S5
interaction.updates-operation=collect-background
interaction.updates-material-type=background-collection-request
interaction.updates-payload=collect
interaction.updates-sensitivity=S1

# No reasoning mechanism is enabled by first-run bootstrap. Provider endpoints, models,
# credentials, Privacy and provider-owned configuration remain explicit owner/provider choices.