package io.github.didacll.madre.kernel.reasoning;

/** Observed reachability of a reasoning mechanism. UNKNOWN is never selectable. */
public enum ReasoningAvailability {
    UNKNOWN,
    AVAILABLE,
    UNAVAILABLE
}
