# Superseded Packet

This B-series packet is not active guidance.

Re-derive agent-family decisions from the accepted class inventory and UML registry. Preserve the SystemAgent exception only as a module-bound main-agent role.
