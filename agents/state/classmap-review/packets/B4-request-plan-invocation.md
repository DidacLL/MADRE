# Superseded Packet

This B-series packet is not active guidance.

Re-derive request, plan, task, and invocation decisions from the accepted runtime flow. `ReasoningTask` is plan-level and non-executing; `AgentRequest` is constructed at invocation time.
