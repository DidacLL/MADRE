# Classmap Review State

This area is not product authority.

Use `docs/classmap-baseline.md` as the basic class/entity design pressure and `docs/tex/MADRE-AgenticSystem.tex` as the synchronized human-facing runtime domain model.

The previous B-series packets are superseded as active guidance. They may not be used as source wording, source terminology, or source claims. Any future classmap work must re-derive decisions from:

- the MADRE philosophy and source policy in `agents/def/product-orientation.md`;
- the accepted class inventory in `docs/classmap-baseline.md`;
- implementation translation: class, relation, field, operation, invariant, runtime flow, or explicit open question.

Preserve the mature basic system entities and UML unless class-level reasoning rejects a specific relation or responsibility. Remove prose that only sounds engineered.
